name: 3D Edit Component Authoring

on:
  pull_request:
    paths:
      - '.github/workflows/topology-edit-component-authoring.yml'
      - 'public/fixtures/topology-edit-professional-spec-catalog.json'
      - 'src/workspace/topology-edit-3d-authoring-controller.js'
      - 'src/workspace/topology-edit/authoring/topology-edit-authoring-session.js'
      - 'src/workspace/topology-edit/authoring/topology-edit-authoring-operation-planner.js'
      - 'src/workspace/topology-edit/authoring/topology-edit-authoring-inline-component.js'
      - 'src/workspace/viewport-productivity/topology-edit-component-authoring-runtime.js'
      - 'src/workspace/topology-edit/professional/topology-edit-inline-component-operation.js'
      - 'src/workspace/topology-edit/topology-edit-inline-component-command.js'
      - 'tests/topology-edit-component-authoring.test.mjs'
      - 'tests/topology-edit-authoring-session.test.mjs'
      - 'tests/topology-edit-authoring-planner.test.mjs'
      - 'tests/topology-edit-inline-component-insertion.test.mjs'
      - 'e2e/topology-edit-component-authoring.spec.js'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: topology-edit-component-authoring-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  component-authoring:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      TOPOLOGY_EDIT_TARGET_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      PLAYWRIGHT_BROWSER_CHANNEL: chromium

    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TOPOLOGY_EDIT_TARGET_HEAD_SHA }}
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies and Chromium
        run: |
          set -euo pipefail
          npm ci
          npx playwright install --with-deps chromium

      - name: Verify exact component authoring sources
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TOPOLOGY_EDIT_TARGET_HEAD_SHA"
          node --check src/workspace/topology-edit-3d-authoring-controller.js
          node --check src/workspace/topology-edit/authoring/topology-edit-authoring-session.js
          node --check src/workspace/topology-edit/authoring/topology-edit-authoring-operation-planner.js
          node --check src/workspace/topology-edit/authoring/topology-edit-authoring-inline-component.js
          node --check src/workspace/viewport-productivity/topology-edit-component-authoring-runtime.js
          node --check tests/topology-edit-component-authoring.test.mjs
          node --check e2e/topology-edit-component-authoring.spec.js
          git diff --check

      - name: Execute governed component authoring contracts
        run: |
          set -o pipefail
          node --test --test-reporter=tap \
            tests/topology-edit-component-authoring.test.mjs \
            tests/topology-edit-authoring-session.test.mjs \
            tests/topology-edit-authoring-planner.test.mjs \
            tests/topology-edit-inline-component-insertion.test.mjs \
            tests/topology-edit-authoring-composite-operation.test.mjs \
            2>&1 | tee topology-edit-component-authoring-test-output.txt

      - name: Execute production Chromium component authoring walkthrough
        run: |
          npx playwright test \
            e2e/topology-edit-component-authoring.spec.js \
            --project=chromium --workers=1 --retries=0 --reporter=line

      - name: Upload component authoring qualification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: topology-edit-component-authoring-${{ github.sha }}-${{ github.run_attempt }}
          path: |
            topology-edit-component-authoring-test-output.txt
            playwright-report
            test-results
          if-no-files-found: warn
          retention-days: 30
