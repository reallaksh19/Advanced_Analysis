name: wp4-load-calc-overlay-integration-patch

on:
  push:
    branches:
      - agent/empirical-result-overlays-wp4
    paths:
      - 'scripts/wp4-load-calc-overlay-integration-patch.mjs'
      - '.github/workflows/wp4-load-calc-overlay-integration-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/empirical-result-overlays-wp4
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/wp4-load-calc-overlay-integration-patch.mjs
      - shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/workspace/load-calc-consumer-controller.js src/workspace/bootstrap.js
          git diff --cached --quiet && exit 0
          git commit -m 'Wire empirical result overlays into Load Calc 3D'
          git push origin HEAD:agent/empirical-result-overlays-wp4
