name: M035 M036 BM4 Integration Qualification

on:
  push:
    branches:
      - m035-m036-bm4-integration
      - agent/m035-bm4-bend-scope-isolation
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout exact integration head
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      - name: Syntax
        run: |
          node --check scripts/lfea-bm4-local-force-reference-normalization.mjs
          node --check scripts/lfea-bm4-local-force-reference-normalization-check.mjs
          node --check scripts/lfea-bm4-thermal-expansion-authority.mjs
          node --check scripts/lfea-bm4-thermal-expansion-authority-check.mjs
          node --check scripts/lfea-m035-m036-bm4-integration-runtime.mjs
          node --check scripts/lfea-m035-m036-bm4-integration-check.mjs
          node --check scripts/lfea-m035-m036-bm4-comparison.mjs
          node --check scripts/lfea-m035-m036-bm4-failure-attribution.mjs

      - name: Qualify CAESAR local-result reference normalization
        run: node scripts/lfea-bm4-local-force-reference-normalization-check.mjs

      - name: Qualify BM4 thermal expansion authority
        run: node scripts/lfea-bm4-thermal-expansion-authority-check.mjs

      - name: Combined M035 M036 BM4 qualification
        shell: bash
        run: |
          set +e
          node scripts/lfea-m035-m036-bm4-integration-check.mjs > /tmp/m035-m036-integration.log 2>&1
          status=$?
          cat /tmp/m035-m036-integration.log
          if [ "$status" -ne 0 ]; then
            message="$(tr '\n' ' ' < /tmp/m035-m036-integration.log | head -c 3500)"
            echo "::error file=scripts/lfea-m035-m036-bm4-integration-check.mjs,line=1,title=M035 M036 combined BM4 qualification::${message}"
          fi
          exit "$status"

      - name: Real CASE 19-21 combined comparison
        shell: bash
        run: |
          set +e
          node scripts/lfea-m035-m036-bm4-comparison.mjs > /tmp/m035-m036-comparison.log 2>&1
          status=$?
          cat /tmp/m035-m036-comparison.log
          if [ "$status" -ne 0 ]; then
            message="$(tr '\n' ' ' < /tmp/m035-m036-comparison.log | head -c 3500)"
            echo "::error file=scripts/lfea-m035-m036-bm4-comparison.mjs,line=1,title=M035 M036 CASE 19-21 comparison::${message}"
          fi
          exit "$status"

      - name: Row-level remaining-failure attribution
        shell: bash
        run: |
          set +e
          node scripts/lfea-m035-m036-bm4-failure-attribution.mjs > /tmp/m035-m036-attribution.log 2>&1
          status=$?
          cat /tmp/m035-m036-attribution.log
          if [ "$status" -ne 0 ]; then
            message="$(tr '\n' ' ' < /tmp/m035-m036-attribution.log | head -c 3500)"
            echo "::error file=scripts/lfea-m035-m036-bm4-failure-attribution.mjs,line=1,title=M035 M036 BM4 failure attribution::${message}"
          fi
          exit "$status"

      - name: Regression M035 focused feature solve
        run: node scripts/lfea-m035-bm4-feature-solve-check.mjs

      - name: Regression M036
        run: npm run check:lfea-m036

      - name: Regression BM1
        run: npm run check:lfea-bm1-cii-comparison

      - name: Regression BM2
        run: node scripts/lfea-b3.26-bm2-solve-comparison-check.mjs

      - name: Regression BM3
        run: node scripts/lfea-m028-bm3-check.mjs

      - name: Upload integration evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m035-m036-bm4-integration-${{ github.sha }}
          path: |
            /tmp/m035-m036-integration.log
            /tmp/m035-m036-comparison.log
            /tmp/m035-m036-attribution.log
            reports/m035-m036-bm4-cases-19-20-21.json
            reports/m035-m036-bm4-failure-attribution.json
          if-no-files-found: warn
          retention-days: 14
