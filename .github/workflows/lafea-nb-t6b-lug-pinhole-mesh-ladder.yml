name: LAFEA NB-T6B Lug-Pinhole Mesh Ladder

on:
  pull_request:
    paths:
      - '.github/workflows/lafea-nb-t6b-lug-pinhole-mesh-ladder.yml'
      - 'src/core/lafea-meshing/lug-pinhole-t6.js'
      - 'src/core/lafea-meshing/index.js'
      - 'src/workspace/lafea-lug-pinhole-mesh-ladder.js'
      - 'src/workspace/lafea-controlled-continuum-public.js'
      - 'src/workspace/lafea-analysis-mesh-*.js'
      - 'scripts/lafea-nb-t6b-lug-pinhole-mesh-ladder-check.mjs'
      - 'scripts/lafea-nb-t6b-public-surface-check.mjs'
      - 'scripts/lafea-nonbucket-stack-check.mjs'
      - 'docs/LAFEA_NonBucket_NB-T6B_Lug_Pinhole_Mesh_Ladder.md'
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lafea-nb-t6b-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  qualify:
    name: NB-T6B exact-head production mesh qualification
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      PR_BASE_SHA: ${{ github.event.pull_request.base.sha || github.sha }}
    steps:
      - name: Check out exact candidate head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          show-progress: false

      - name: Assert exact checkout and base ancestry
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD_SHA"
          git merge-base --is-ancestor "$PR_BASE_SHA" "$EXPECTED_HEAD_SHA"
          test -z "$(git status --porcelain=v1 --untracked-files=no)"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Run NB-T6B production mesh qualification
        shell: bash
        run: |
          set -o pipefail
          node scripts/lafea-nb-t6b-lug-pinhole-mesh-ladder-check.mjs \
            2>&1 | tee lafea-nb-t6b-lug-pinhole-mesh-ladder.log

      - name: Run NB-T6B public-surface qualification
        run: node scripts/lafea-nb-t6b-public-surface-check.mjs

      - name: Run retained T6 meshing qualification
        run: node scripts/lafea.10-t6-generation-check.mjs

      - name: Run retained NB-T4A mesh-evidence qualification
        run: node scripts/lafea-nb-t4a-analysis-mesh-evidence-check.mjs

      - name: Run retained B7D controller qualification
        run: node scripts/lafea-template-b7d-controlled-continuum-controller-check.mjs

      - name: Run non-bucket stack
        run: npm run check:lafea-nonbucket-stack

      - name: Run meshing aggregate
        run: npm run check:lafea-meshing

      - name: Run strict syntax checks
        run: npm run syntax:strict

      - name: Run import checks
        run: npm run check:imports

      - name: Build production bundle
        run: npm run build

      - name: Verify patch hygiene and clean tracked tree
        shell: bash
        run: |
          set -euo pipefail
          git diff --check "$PR_BASE_SHA...HEAD"
          test -z "$(git status --porcelain=v1 --untracked-files=no)"

      - name: Upload exact-head NB-T6B evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lafea-nb-t6b-${{ github.event.pull_request.head.sha || github.sha }}
          path: lafea-nb-t6b-lug-pinhole-mesh-ladder.log
          if-no-files-found: error
          retention-days: 30
