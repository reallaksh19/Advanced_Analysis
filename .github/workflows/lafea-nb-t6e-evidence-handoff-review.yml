name: LAFEA NB-T6E Evidence Handoff Review

on:
  pull_request:
    paths:
      - '.github/workflows/lafea-nb-t6e-evidence-handoff-review.yml'
      - 'src/workspace/lafea-selected-pilot-evidence-handoff.js'
      - 'src/workspace/lafea-b7d-recovery-render-bridge.js'
      - 'src/workspace/lafea-load-driven-pilot-qualification.js'
      - 'src/workspace/lafea-controlled-continuum-public.js'
      - 'src/workspace/lafea-lug-pinhole-physical-problem-*.js'
      - 'src/workspace/lafea-controlled-continuum-*.js'
      - 'src/workspace/lafea-recovery-render-*.js'
      - 'src/core/local-continuum/**'
      - 'scripts/lafea-nb-t6e-evidence-handoff-review-check.mjs'
      - 'scripts/lafea-nb-t6d-load-driven-qualification-check.mjs'
      - 'scripts/lafea-nb-t6d-b7d-recovery-render-bridge-check.mjs'
      - 'scripts/lafea-nb-t6c-fixture.mjs'
      - 'scripts/lafea-nb-t6c-physical-problem-batch-check.mjs'
      - 'scripts/lafea-template-b7d-controlled-continuum-controller-check.mjs'
      - 'scripts/lafea-nb-t4b-recovery-render-check.mjs'
      - 'scripts/lafea-nonbucket-stack-check.mjs'
      - 'docs/LAFEA_NonBucket_NB-T6E_Selected_Pilot_Evidence_Handoff.md'
      - 'package.json'
      - 'package-lock.json'
  push:
    branches: [main]
    paths:
      - '.github/workflows/lafea-nb-t6e-evidence-handoff-review.yml'
      - 'src/workspace/lafea-selected-pilot-evidence-handoff.js'
      - 'src/workspace/lafea-b7d-recovery-render-bridge.js'
      - 'src/workspace/lafea-load-driven-pilot-qualification.js'
      - 'src/workspace/lafea-controlled-continuum-public.js'
      - 'src/workspace/lafea-lug-pinhole-physical-problem-*.js'
      - 'src/workspace/lafea-controlled-continuum-*.js'
      - 'src/workspace/lafea-recovery-render-*.js'
      - 'src/core/local-continuum/**'
      - 'scripts/lafea-nb-t6e-evidence-handoff-review-check.mjs'
      - 'scripts/lafea-nb-t6d-load-driven-qualification-check.mjs'
      - 'scripts/lafea-nb-t6d-b7d-recovery-render-bridge-check.mjs'
      - 'scripts/lafea-nb-t6c-fixture.mjs'
      - 'scripts/lafea-nonbucket-stack-check.mjs'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lafea-nb-t6e-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  qualify:
    name: NB-T6E selected-pilot evidence handoff and review
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    env:
      EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      PR_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before || github.sha }}
    steps:
      - name: Check out exact candidate head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          show-progress: false

      - name: Assert exact checkout and base ancestry
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD_SHA"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git merge-base --is-ancestor "$PR_BASE_SHA" "$EXPECTED_HEAD_SHA"
          fi
          test -z "$(git status --porcelain=v1 --untracked-files=no)"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Run NB-T6E evidence handoff qualification
        shell: bash
        run: |
          set -o pipefail
          node scripts/lafea-nb-t6e-evidence-handoff-review-check.mjs \
            2>&1 | tee lafea-nb-t6e-evidence-handoff-review.log

      - name: Run retained NB-T6D load-driven qualification
        run: node scripts/lafea-nb-t6d-load-driven-qualification-check.mjs

      - name: Run retained NB-T6D recovery-render bridge qualification
        run: node scripts/lafea-nb-t6d-b7d-recovery-render-bridge-check.mjs

      - name: Run retained NB-T6C batch qualification
        run: node scripts/lafea-nb-t6c-physical-problem-batch-check.mjs

      - name: Run retained B7D controller qualification
        run: node scripts/lafea-template-b7d-controlled-continuum-controller-check.mjs

      - name: Run retained NB-T4B render-boundary qualification
        run: node scripts/lafea-nb-t4b-recovery-render-check.mjs

      - name: Run non-bucket aggregate
        run: npm run check:lafea-nonbucket-stack

      - name: Run strict syntax checks
        run: npm run syntax:strict

      - name: Run import checks
        run: npm run check:imports

      - name: Build production bundle
        run: npm run build

      - name: Verify patch hygiene and clean tracked tree
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git diff --check "$PR_BASE_SHA...HEAD"
          else
            git diff --check HEAD^
          fi
          test -z "$(git status --porcelain=v1 --untracked-files=no)"

      - name: Upload exact-head NB-T6E evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lafea-nb-t6e-${{ github.event.pull_request.head.sha || github.sha }}
          path: lafea-nb-t6e-evidence-handoff-review.log
          if-no-files-found: error
          retention-days: 30
