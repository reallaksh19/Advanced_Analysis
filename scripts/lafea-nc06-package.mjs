import { createHash } from 'node:crypto';import { readFile } from 'node:fs/promises';import { resolve } from 'node:path';import { executeNc06 } from './nc06-package/benchmarks.mjs';
const [upstreamPath,outDir,head]=process.argv.slice(2);if(!upstreamPath||!outDir||!head)throw Error('usage: upstream out head');
const files=['scripts/lafea-nc06-package.mjs','scripts/nc06-package/config.mjs','scripts/nc06-package/engine.mjs','scripts/nc06-package/oracle.mjs','scripts/nc06-package/evidence.mjs','scripts/nc06-package/benchmarks.mjs','src/core/nonlinear-shell-contact/code-assessment-package-contract.js','src/core/nonlinear-shell-contact/code-assessment-package-qualification-evaluator.js'];const h=createHash('sha256');for(const f of files)h.update(f).update('\0').update(await readFile(resolve(f)));const implementationHash=`sha256:${h.digest('hex')}`;const upstreamReceipt=JSON.parse(await readFile(upstreamPath));const result=await executeNc06({outDir,exactHeadSha:head,implementationHash,upstreamReceipt});console.log(JSON.stringify({status:result.summary.status,implementationHash,evidence:result.records.length,basisHash:result.basis.basisHash}));
