import assert from 'node:assert/strict'; import {mkdir,writeFile} from 'node:fs/promises'; import {resolve} from 'node:path';
import {semanticHash} from '../src/core/nonlinear-shell-contact/contracts.js';
import {DEFAULT_PLASTIC_MATERIAL_CONTRACT,REQUIRED_PLASTIC_MATERIAL_EVIDENCE,validatePlasticMaterialContract} from '../src/core/nonlinear-shell-contact/plastic-material-contract.js';
import {evaluatePlasticMaterialQualification} from '../src/core/nonlinear-shell-contact/plastic-material-qualification-evaluator.js';
import {NC04_CONTRACT_FIXTURES} from '../src/core/nonlinear-shell-contact/nc04-fixtures.js'; import {runNc04NegativeControls} from '../src/core/nonlinear-shell-contact/nc04-negative-controls.js';
const arg=process.argv.find((entry)=>entry.startsWith('--output-dir='));const outputDir=resolve(arg?.slice('--output-dir='.length)||'artifacts/lafea-nc04');await mkdir(outputDir,{recursive:true});for(const fixture of NC04_CONTRACT_FIXTURES)validatePlasticMaterialContract(fixture.contract);const negatives=runNc04NegativeControls();assert.ok(negatives.length>=20);assert.ok(negatives.every((entry)=>entry.passed));const qualification=evaluatePlasticMaterialQualification({contract:DEFAULT_PLASTIC_MATERIAL_CONTRACT});assert.equal(qualification.status,'NC04_BLOCKED');assert.equal(qualification.authority.nc04ContractQualified,true);assert.equal(qualification.authority.plasticMaterialQualified,false);assert.equal(qualification.authority.plasticDentingProcedureQualified,false);const report={schema:'nonlinear-shell-contact-nc04-contract-run/v1',status:qualification.status,plasticMaterialContractHash:DEFAULT_PLASTIC_MATERIAL_CONTRACT.plasticMaterialContractHash,evidenceDomainCount:REQUIRED_PLASTIC_MATERIAL_EVIDENCE.length,fixtureCount:NC04_CONTRACT_FIXTURES.length,negativeControlCount:negatives.length,qualification};const sealed={...report,runSemanticHash:semanticHash(report)};await writeFile(resolve(outputDir,'nc04-report.json'),`${JSON.stringify(sealed,null,2)}\n`,'utf8');await writeFile(resolve(outputDir,'nc04-contract.json'),`${JSON.stringify(DEFAULT_PLASTIC_MATERIAL_CONTRACT,null,2)}\n`,'utf8');console.log(JSON.stringify(sealed));
