import { createHash } from 'node:crypto';export const hash=(v)=>`sha256:${createHash('sha256').update(Buffer.isBuffer(v)?v:Buffer.from(v)).digest('hex')}`;const canon=(v)=>JSON.stringify(Array.isArray(v)?v.map(x=>JSON.parse(canon(x))):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,JSON.parse(canon(v[k]))])):v);export const semanticHash=(v)=>hash(canon(v));export function seal(v,f='evidenceHash'){const c=structuredClone(v);delete c[f];return Object.freeze({...c,[f]:semanticHash(c)});}
