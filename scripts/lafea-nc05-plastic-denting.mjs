import{readFile}from'node:fs/promises';import{resolve}from'node:path';import{createHash}from'node:crypto';import{executeNc05}from'./nc05-denting/benchmarks.mjs';const[solver,outDir,head]=process.argv.slice(2);if(!solver||!outDir||!head)throw Error('usage: solver outDir exactHeadSha');const files=['scripts/lafea-nc05-plastic-denting.mjs','scripts/nc05-denting/config.mjs','scripts/nc05-denting/engine.mjs','scripts/nc05-denting/evidence.mjs','scripts/nc05-denting/benchmarks.mjs','src/core/nonlinear-shell-contact/plastic-denting-procedure-contract.js','src/core/nonlinear-shell-contact/plastic-denting-qualification-evaluator.js'];const h=createHash('sha256');for(const f of files)h.update(f).update('\0').update(await readFile(resolve(f)));const implementationHash=`sha256:${h.digest('hex')}`;const x=await executeNc05({solver,outDir,exactHeadSha:head,implementationHash});console.log(JSON.stringify({status:x.summary.status,implementationHash,evidence:x.records.length}));
