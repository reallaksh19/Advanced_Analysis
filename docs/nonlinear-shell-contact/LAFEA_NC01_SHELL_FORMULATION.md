# LAFEA-NC NC-01 Shell Formulation Qualification

## Current-main replacement scope

This package replaces stale PR #651 without importing its duplicated NC-00 foundation. It is based on the live `main` descendant of merge `18e259f8a18e9011482d3ca4d1b8bd51dbe986f4` and consumes the retained qualified bridge artifact from run `31021329297`, artifact `8936729914`, digest `sha256:2b1458c447724d15a2e9178af8302cba91b78593854f8741ffc57a9f0646d28d`.

## Implemented controls

- finite-rotation, small-strain Reissner–Mindlin contract;
- five physical DOFs and numerical-only drilling stabilization;
- midsurface and explicit-offset convention;
- follower current-surface pressure;
- fixed-physical-coordinate section integration-point recovery;
- explicit prohibition of nodal display averaging and nearest-node qualification;
- eight governed shell benchmark identities;
- strict physical global and probe-local mesh refinement;
- reference uncertainty, equilibrium, energy, hourglass and transverse-shear controls;
- negative-mutation detection for objectivity, locking, section sign, mapping, follower loading, normal orientation and hourglass behavior;
- semantically sealed NC-00 upstream binding;
- rejection of caller-created PASS, authority and disposition fields;
- two deterministic exact-head evaluations with diagnostic retention on failure.

The synthetic qualified fixture exercises only the evaluator path. It is not programme numerical evidence and is never accepted by the exact-head workflow as external solver evidence.

## Required real evidence

The phase cannot qualify until all eight `NC01-SH-01` through `NC01-SH-08` packages are generated by real CalculiX 2.22 execution and independently verified. Each evidence object must bind the exact candidate head and retain raw deck/output hashes, independent reference and oracle hashes, four physical mesh levels, fixed-coordinate recovery, residuals, energies and the required negative mutation.

## Authority boundary

A qualified report may set only:

```text
nc01ContractQualified = true
shellFormulationQualified = true
nc02Authorized = true
```

It must retain contact, denting, plasticity, code, module, production, fitness-for-service, remaining-strength, automatic-acceptance and autonomous-disposition authority as false.
