/** Pure deterministic reducer for the ten governed topology-edit commands. */
import { deepFreeze } from '../../core/shared-piping-model/index.js';
import { deterministicTopologyEditId } from './topology-edit-command-contract.js';
import { assertResolvedTopologyEditCommand } from './topology-edit-command-resolver.js';
import { assertCanonicalTopologyHash, canonicalTopologyStateHash, finalizeCanonicalTopology } from './topology-edit-canonical-state.js';

function generatedId(prefix,commandId,role){return`${prefix}:${deterministicTopologyEditId(commandId,role).split(':').at(-1)}`;}
function cloneRows(rows){return JSON.parse(JSON.stringify(rows??[]));}
function exactIndex(rows,id,label){const indexes=rows.map((row,index)=>row?.id===id?index:-1).filter((index)=>index>=0);if(indexes.length!==1)throw new RangeError(`TopologyEditPureReducer: ${label} ${id} resolved ${indexes.length} records.`);return indexes[0];}
function assertUnusedId(topology,id){const collision=['nodes','edges','junctions','supports','boundaries','rigids'].some((key)=>(topology[key]??[]).some((record)=>record?.id===id));if(collision)throw new Error(`TopologyEditPureReducer: generated identity collision ${id}.`);}
function replaceNodeReferences(record,sourceNodeId,targetNodeId){const result={...record};for(const key of ['nodeId','fromNodeId','toNodeId'])if(result[key]===sourceNodeId)result[key]=targetNodeId;if(Array.isArray(result.nodeIds))result.nodeIds=[...new Set(result.nodeIds.map((id)=>id===sourceNodeId?targetNodeId:id))];return result;}
function moveNode(topology,command){const nodes=cloneRows(topology.nodes);const index=exactIndex(nodes,command.payload.nodeId,'node');nodes[index]={...nodes[index],position:{...command.payload.position}};return{...topology,nodes};}
function remapCollection(rows,sourceNodeId,targetNodeId){return cloneRows(rows).map((row)=>replaceNodeReferences(row,sourceNodeId,targetNodeId));}
function mergeNodes(topology,command){const{sourceNodeId,targetNodeId}=command.payload;const nodes=cloneRows(topology.nodes);const source=nodes[exactIndex(nodes,sourceNodeId,'source node')];const target=nodes[exactIndex(nodes,targetNodeId,'target node')];const mergedTarget={...target,portKeys:[...new Set([...(target.portKeys??[]),...(source.portKeys??[])])].sort()};return{...topology,nodes:nodes.filter((node)=>node.id!==sourceNodeId).map((node)=>node.id===targetNodeId?mergedTarget:node),edges:remapCollection(topology.edges,sourceNodeId,targetNodeId),junctions:remapCollection(topology.junctions,sourceNodeId,targetNodeId),supports:remapCollection(topology.supports,sourceNodeId,targetNodeId),boundaries:remapCollection(topology.boundaries,sourceNodeId,targetNodeId),rigids:remapCollection(topology.rigids,sourceNodeId,targetNodeId)};}
function addEdge(topology,command){const role=command.commandType==='BRIDGE_GAP'?'bridge-edge':'straight-edge';const edgeId=generatedId('edge',command.commandId,role);assertUnusedId(topology,edgeId);const edge={id:edgeId,componentKey:null,fromNodeId:command.payload.fromNodeId,toNodeId:command.payload.toNodeId,diameterMm:command.payload.diameterMm,entityType:command.payload.entityType,sourcePath:null,createdByCommandId:command.commandId};return{...topology,edges:[...cloneRows(topology.edges),edge]};}
function splitEdge(topology,command){const edges=cloneRows(topology.edges);const edge=edges[exactIndex(edges,command.payload.edgeId,'edge')];const nodes=cloneRows(topology.nodes);const from=nodes[exactIndex(nodes,edge.fromNodeId,'FROM node')];const to=nodes[exactIndex(nodes,edge.toNodeId,'TO node')];const delta={x:to.position.x-from.position.x,y:to.position.y-from.position.y,z:to.position.z-from.position.z};if(!(Math.hypot(delta.x,delta.y,delta.z)>0))throw new RangeError(`TopologyEditPureReducer: SPLIT_EDGE ${edge.id} has zero length.`);const nodeId=generatedId('node',command.commandId,'split-node');const leftEdgeId=generatedId('edge',command.commandId,'split-left-edge');const rightEdgeId=generatedId('edge',command.commandId,'split-right-edge');[nodeId,leftEdgeId,rightEdgeId].forEach((id)=>assertUnusedId(topology,id));const position={x:from.position.x+delta.x*command.payload.fraction,y:from.position.y+delta.y*command.payload.fraction,z:from.position.z+delta.z*command.payload.fraction};const common={diameterMm:edge.diameterMm??null,entityType:edge.entityType??'PIPE',sourcePath:edge.sourcePath??null,createdByCommandId:command.commandId,derivedFromEdgeId:edge.id,sourceComponentKey:edge.componentKey??edge.sourceComponentKey??null};const node={id:nodeId,position,portKeys:[],createdByCommandId:command.commandId,derivedFromEdgeId:edge.id};const left={...edge,...common,id:leftEdgeId,fromNodeId:edge.fromNodeId,toNodeId:nodeId,componentKey:edge.componentKey??null};const right={...edge,...common,id:rightEdgeId,fromNodeId:nodeId,toNodeId:edge.toNodeId,componentKey:null};return{...topology,nodes:[...nodes,node],edges:[...edges.filter((row)=>row.id!==edge.id),left,right]};}
function disconnectEndpoint(topology,command){const edges=cloneRows(topology.edges);const edgeIndex=exactIndex(edges,command.payload.edgeId,'edge');const edge=edges[edgeIndex];const endpointKey=command.payload.endpoint==='FROM'?'fromNodeId':'toNodeId';const originalNodeId=edge[endpointKey];const nodes=cloneRows(topology.nodes);const nodeIndex=exactIndex(nodes,originalNodeId,'endpoint node');const originalNode=nodes[nodeIndex];const newNodeId=generatedId('node',command.commandId,`disconnect-${command.payload.endpoint.toLowerCase()}-node`);assertUnusedId(topology,newNodeId);const movedPortKeys=[...(command.targets.endpointPortKeys??[])];const moved=new Set(movedPortKeys);nodes[nodeIndex]={...originalNode,portKeys:(originalNode.portKeys??[]).filter((key)=>!moved.has(key))};nodes.push({id:newNodeId,position:{...originalNode.position},portKeys:movedPortKeys,createdByCommandId:command.commandId,derivedFromNodeId:originalNodeId});edges[edgeIndex]={...edge,[endpointKey]:newNodeId};return{...topology,nodes,edges};}
function deleteEdge(topology,command){const edges=cloneRows(topology.edges);exactIndex(edges,command.payload.edgeId,'edge');return{...topology,edges:edges.filter((edge)=>edge.id!==command.payload.edgeId)};}
function addBendDefinition(topology,command){const edgeIds=new Set(command.payload.edgeIds);const edges=cloneRows(topology.edges).map((edge)=>edgeIds.has(edge.id)?{...edge,bendDefinition:{nodeId:command.payload.nodeId,radiusMm:command.payload.radiusMm,bendType:command.payload.bendType,createdByCommandId:command.commandId}}:edge);return{...topology,edges};}
function otherEndpoint(edge,nodeId){return edge.fromNodeId===nodeId?edge.toNodeId:edge.fromNodeId;}
function addJunctionDefinition(topology,command){const junctionId=generatedId('junction',command.commandId,'junction-definition');assertUnusedId(topology,junctionId);const edgeMap=new Map((topology.edges??[]).map((edge)=>[edge.id,edge]));const nodeIds=[command.payload.nodeId,...command.payload.edgeIds.map((id)=>otherEndpoint(edgeMap.get(id),command.payload.nodeId))];const junction={id:junctionId,componentKey:null,nodeIds:[...new Set(nodeIds)].sort(),edgeIds:[...command.payload.edgeIds],entityType:command.payload.junctionType,createdByCommandId:command.commandId};return{...topology,junctions:[...cloneRows(topology.junctions),junction]};}
function trimEdge(topology,command){const edges=cloneRows(topology.edges);const edge=edges[exactIndex(edges,command.payload.edgeId,'edge')];const endpointId=command.payload.endpoint==='FROM'?edge.fromNodeId:edge.toNodeId;const otherId=command.payload.endpoint==='FROM'?edge.toNodeId:edge.fromNodeId;const nodes=cloneRows(topology.nodes);const endpointIndex=exactIndex(nodes,endpointId,'trim endpoint node');const other=nodes[exactIndex(nodes,otherId,'trim other node')];const endpoint=nodes[endpointIndex];const fraction=command.payload.fraction;nodes[endpointIndex]={...endpoint,position:{x:endpoint.position.x+(other.position.x-endpoint.position.x)*fraction,y:endpoint.position.y+(other.position.y-endpoint.position.y)*fraction,z:endpoint.position.z+(other.position.z-endpoint.position.z)*fraction},trimmedByCommandId:command.commandId};return{...topology,nodes};}
const REDUCERS=Object.freeze({MOVE_NODE:moveNode,MERGE_NODES:mergeNodes,BRIDGE_GAP:addEdge,ADD_STRAIGHT_ELEMENT:addEdge,SPLIT_EDGE:splitEdge,DISCONNECT_ENDPOINT:disconnectEndpoint,DELETE_EDGE:deleteEdge,ADD_BEND_DEFINITION:addBendDefinition,ADD_JUNCTION_DEFINITION:addJunctionDefinition,TRIM_EDGE:trimEdge});
export function applyResolvedTopologyEditCommand(canonicalTopology,resolvedCommandInput){assertCanonicalTopologyHash(canonicalTopology);const command=assertResolvedTopologyEditCommand(resolvedCommandInput);const currentHash=canonicalTopologyStateHash(canonicalTopology);if(command.basis.priorDraftHash!==currentHash)throw new Error('TopologyEditPureReducer: resolved command is stale for the current canonical topology.');const reducer=REDUCERS[command.commandType];if(!reducer)throw new RangeError(`TopologyEditPureReducer: unsupported command ${command.commandType}.`);return finalizeCanonicalTopology(reducer(JSON.parse(JSON.stringify(canonicalTopology)),command));}
export function replayResolvedTopologyEditCommands(baseCanonicalTopology,commands=[]){const base=deepFreeze(JSON.parse(JSON.stringify(baseCanonicalTopology)));return commands.reduce((topology,command)=>applyResolvedTopologyEditCommand(topology,command),base);}
